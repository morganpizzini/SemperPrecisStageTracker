name: Web API

on:
  push:
    branches: [ main ]
  workflow_dispatch:  

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.x
    - name: Install dependencies
      run: dotnet restore ./SemperPrecisStageTracker.Blazor/SemperPrecisStageTracker.Blazor.csproj
    - name: Build
      run: dotnet build ./SemperPrecisStageTracker.Blazor/SemperPrecisStageTracker.Blazor.csproj --configuration Release --no-restore

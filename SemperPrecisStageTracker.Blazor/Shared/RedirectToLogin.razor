@inject NavigationManager Navigation
@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo(
            $"app/login?returnUrl={Uri.EscapeDataString(Navigation.Uri)}");
    }
}
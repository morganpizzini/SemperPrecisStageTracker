@page "/logout"
@inject IAuthenticationService AuthenticationService

@code {
    protected override void OnInitialized()
    {
        AuthenticationService.Logout(RouteHelper.GetUrl<Login>());
    }
}